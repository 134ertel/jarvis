"""Shared infrastructure used across all backend modules: configuration and logging.

Nothing here is module-specific — it holds no AI, voice, control, memory, settings,
or security logic of its own.
"""
